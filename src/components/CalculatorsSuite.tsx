import React from 'react';
import BmiCalculatorComponent from './calculators/BmiCalculator';
import BmrCalculatorComponent from './calculators/BmrCalculator';
import CalorieCalculatorComponent from './calculators/CalorieCalculator';
import ProteinCalculatorComponent from './calculators/ProteinCalculator';
import WaterCalculatorComponent from './calculators/WaterCalculator';
import BodyFatCalculatorComponent from './calculators/BodyFatCalculator';
import LeanBodyMassCalculatorComponent from './calculators/LeanBodyMassCalculator';
import IdealWeightCalculatorComponent from './calculators/IdealWeightCalculator';
import MacroCalculatorComponent from './calculators/MacroCalculator';
import WalkingCaloriesCalculatorComponent from './calculators/WalkingCaloriesCalculator';

/* ==========================================================
   1. BMI CALCULATOR COMPONENT EXPORT
   ========================================================== */
export function BmiCalculator() {
  return <BmiCalculatorComponent />;
}

/* ==========================================================
   2. BMR CALCULATOR COMPONENT EXPORT
   ========================================================== */
export function BmrCalculator() {
  return <BmrCalculatorComponent />;
}

/* ==========================================================
   3. CALORIE CALCULATOR COMPONENT EXPORT
   ========================================================== */
export function CalorieCalculator() {
  return <CalorieCalculatorComponent />;
}

/* ==========================================================
   4. PROTEIN CALCULATOR COMPONENT EXPORT
   ========================================================== */
export function ProteinCalculator() {
  return <ProteinCalculatorComponent />;
}

/* ==========================================================
   5. WATER CALCULATOR COMPONENT EXPORT
   ========================================================== */
export function WaterCalculator() {
  return <WaterCalculatorComponent />;
}

/* ==========================================================
   6. BODY FAT CALCULATOR COMPONENT EXPORT
   ========================================================== */
export function BodyFatCalculator() {
  return <BodyFatCalculatorComponent />;
}

/* ==========================================================
   7. LEAN BODY MASS CALCULATOR COMPONENT EXPORT
   ========================================================== */
export function LeanBodyMassCalculator() {
  return <LeanBodyMassCalculatorComponent />;
}

/* ==========================================================
   8. IDEAL WEIGHT CALCULATOR COMPONENT EXPORT
   ========================================================== */
export function IdealWeightCalculator() {
  return <IdealWeightCalculatorComponent />;
}

/* ==========================================================
   9. MACRONUTRIENT CALCULATOR COMPONENT EXPORT
   ========================================================== */
export function MacroCalculator() {
  return <MacroCalculatorComponent />;
}

/* ==========================================================
   10. WALKING CALORIES BURNED CALCULATOR COMPONENT EXPORT
   ========================================================== */
export function WalkingCaloriesCalculator() {
  return <WalkingCaloriesCalculatorComponent />;
}
